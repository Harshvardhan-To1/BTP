numpy>=1.26
scipy>=1.11
pandas>=2.0
matplotlib>=3.8
scikit-learn>=1.3
pytest>=7.0
python-pptx>=0.6.23
tqdm>=4.66
# Only needed for the historical matinv_bench/ benchmark (CPU build is sufficient):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
